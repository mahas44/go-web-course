package main

import (
	cookie "web-state/06_cookies"
)

func main() {

	// http.HandleFunc("/", formurl.Foo)
	// http.HandleFunc("/", formpost.Foo)
	// http.HandleFunc("/", form.Foo)
	// http.HandleFunc("/", formfile.Foo)
	// http.Handle("/favicon.ico", http.NotFoundHandler())
	// http.ListenAndServe(":8080", nil)
	// redirect.Redirect()
	cookie.Cookie()

}
